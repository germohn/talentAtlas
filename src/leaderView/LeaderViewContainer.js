import { connect } from 'react-redux';
import LeaderView  from './LeaderView';

const mapStateToProps = (state, props) => (
  {
  }
);

const mapDispatchToProps = dispatch => (
  {
  }
);

export default connect(mapStateToProps, mapDispatchToProps)(LeaderView);